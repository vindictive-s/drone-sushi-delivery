import React from "react";
import { HeroContainer, HeroBg, ImageBg } from "./Hero.styled";

const HeroSection = () => {
  return (
    <HeroContainer>
      <HeroBg>
        <ImageBg />
      </HeroBg>
    </HeroContainer>
  );
};

export default HeroSection;
